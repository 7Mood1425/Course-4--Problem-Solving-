#include<iostream>
using namespace std;
#include<string>

float ReadTriangal(float &A,float &B) {
	int L;
	cout << "Please enter A ?\n ";
	cin >> A;
	cout << "Please enter B ?\n ";
	cin >> B;
	return A,B;
}

float CircleAreaByITriangal(float A, float B) {
	const float PI = 3.1415926535;
	float Area = PI * (pow(B, 2) / 4) / ((2 * A - B) / (2 * A + B));
	return Area;
}

void PrintResult(float area) {
	cout << "\nCircle area = " << area << endl;
}

int main() {
	float A, B;
	ReadTriangal(A, B);
	PrintResult(CircleAreaByITriangal(A, B));
	return 0;
}

